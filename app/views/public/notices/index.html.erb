<div class='container px-5 px-sm-0 my-5'>
  <div class="row">
    <div class='col-md-2'>
      <h5>ジャンル検索</h5>
      <%= render 'public/notices/genre', genres: @genres %>
    </div>
    <div class="col-md-10"><h1><投稿一覧>(全<%= @notices.count %>件)</h1>
      <div class="row">
        <%= render 'index', notices: @notices %>
      </div>
      <%= paginate @notices %>
    </div>
  </div>
</div>

