<div class="container-fluid">
  <div class="row mb-3">
    <div class="col">
      <h4 class="ml-5">ジャンル新規登録</h4>
    </div>
  </div>
  <%= form_with model: [:admin, @genre] do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <% end %>
</div>